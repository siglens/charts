apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gcp-pd-pvc
  namespace: {{ .Values.global.namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gcp-pd-standard-rwo
  resources:
    requests:
      storage: {{ .Values.pvc.size }}
  